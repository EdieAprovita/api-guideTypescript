NODE_ENV=test
JWT_SECRET=test_jwt_secret_key_for_testing_12345
JWT_REFRESH_SECRET=test_refresh_secret_12345
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# Redis Docker local para tests de integración
REDIS_HOST=localhost
REDIS_PORT=6379

# MongoDB local para tests (cambiar por tu MongoDB local si lo tienes)
# Si no tienes MongoDB local, mantenemos MongoDB Atlas pero con una DB diferente para tests
MONGODB_URI=mongodb+srv://eacm316:ea36153547@vegan-guide.pz8qu.mongodb.net/vegan-guide-test?retryWrites=true&w=majority

# Opcional: MongoDB local si lo tienes corriendo
# MONGODB_URI=mongodb://localhost:27017/vegan-guide-test