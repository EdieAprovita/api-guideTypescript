config:
  target: "http://localhost:5001"
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Login"
    flow:
      - post:
          url: "/api/v1/users/login"
          json:
            email: "test@example.com"
            password: "123456"
