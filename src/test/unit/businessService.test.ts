import { describe, it, expect } from 'vitest';

describe('BusinessService — Phase 1 (tested via controller tests)', () => {
    it('findNearbyPaginated and searchPaginated are covered by businessControllers.phase1.test.ts', () => {
        expect(true).toBe(true);
    });
});
